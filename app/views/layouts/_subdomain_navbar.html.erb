<% unless (controller_name == "schools" && (action_name == "index" || action_name == "notifications" || action_name == "global_chats")) ||  (controller_name == "users" && action_name == "index") || (controller_name == "marketings" || (controller_name == "courses" && (action_name == "index" || action_name == "show" || action_name == "new")) || (controller_name == "announcements") || (controller_name == "calendars")) || (controller_name == "mailbox") || (controller_name == "conversations") || (controller_name == "lessons" && (action_name == "index" || action_name == "show" || action_name == "show_activity")) || (controller_name == "courses" && action_name == "all_gradebooks") || (controller_name == "events" && action_name == "event_show") %>
  <nav class="navbar navbar-default navbar-fixed-top cus-nav-header">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand nav-img-wi" href="#"><%= image_tag 'logo-1.png'%></a>
    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
            <li class="th-log-top">
            <%= link_to 'Log out', destroy_user_session_path, method: 'delete' %>
          </li>
          <% else %>
            <li class="th-log-top"><%= link_to "Log in", new_user_session_path %></li>
          <% end %>
      </ul>
    </div>
  </nav>
<%else%>
  <%= render partial: 'layouts/sub_header' %>
<%end%>


