<body class="ch-bod">
  <div class="container app">
    <div class="row app-one">
      <div class="col-sm-3"></div>
      <div class="col-sm-6 col-sm-offset-3 side">
        <div class="side-one">
          <div class="row heading">
            <div class="col-sm-5 col-xs-5 ">
            </div>
            <div class="col-sm-7 col-xs-7  heading-dot  pull-right">
              <% if (current_user.is_super_admin?) || (current_user.admin?) || (current_user.teacher?) %>
  					  <sapn class="lead"><%= link_to 'New Chat Room', new_chat_room_path, class: 'btn btn-space btn-primary cha-room-btn' %></span>
  					  <% end %>
            </div>
          </div>
          <div class="row sideBar">
            <%= render @chat_rooms %>          
          </div>
        </div>
      </div>
    </div>
  </div>
</body>
