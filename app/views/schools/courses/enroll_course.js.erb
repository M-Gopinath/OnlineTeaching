$(".enroll-course-<%= @course.id %>").html('<button class="btn btn-primary">Enrolled</button>')
$(".enroll-course-link-<%= @course.id %>").html('<%= link_to students_course_path(@course) do %><%= image_tag @course.course_images.last.present? && @course.course_images.last.image.present? && @course.course_images.last.image.url.present? ? @course.course_images.last.image.url : "https://png.icons8.com/material-rounded/100/666666/picture.png" %><%end%>')
