$("#first-lesson").html("<%= j(render partial: 'schools/lesson', locals: {lesson: @new_lesson, teachers: @teachers, lessons: @lessons,course: @lesson.course}) %>")
$(".lesson-course-id").val("<%= @lesson.course_id %>");
$(".lesson-list").html("<%= j(render partial: 'schools/lessons/lesson_lists', locals: {lessons: @lessons, course: @lesson.course}) %>")
// <%= render "schools/lessons/new_activities" %>
lessonDatePicker();
$('.datetimepicker-video').datetimepicker({
  useCurrent: false,
  format: 'LL',
  minDate: moment(new Date("<%= @lesson.start_date.present? ? @lesson.start_date : Time.now.strftime('%Y-%m-%d') %>")).format("LL"),
  maxDate: moment(new Date("<%= @lesson.end_date.present? ? @lesson.end_date : Time.now.strftime('%Y-%m-%d') %>")).format("LL")
});
// if ("<%= @lesson.start_date.present? %>"){
//   var start_date = moment(new Date("<%= @lesson.start_date %>")).format("LL");
//   $('.datetimepicker-video').each(function(){
//     $(this).data('DateTimePicker').minDate(start_date);
//   });
//   // $('.datetimepicker33').data('DateTimePicker').minDate(e.date);
// }
// if("<%= @lesson.end_date.present? %>"){
// 	var end_date = moment(new Date("<%= @lesson.end_date %>")).format("LL");
//   $('.datetimepicker-video').each(function(){
//     $(this).data('DateTimePicker').maxDate(end_date);
//   });
// }
$('.th-vido-ac-area').data('start-date',"<%= @lesson.start_date.present? ? @lesson.start_date : @lessons.first.start_date %>");
$('.th-vido-ac-area').data('end-date',"<%= @lesson.end_date.present? ? @lesson.end_date : @lessons.first.end_date %>");

$(".th-add-less").css("background-color", "#1EC09F");
$(".tab4").click();
// $("#lesson_"+"<%= @lesson.id %> .th-child-act .th-add-less").css("background-color", "#337ab7");